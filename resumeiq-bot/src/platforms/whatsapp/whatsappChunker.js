function chunkMessage(text,limit=1500){if(text.length<=limit)return [text];const parts=[];let rest=text;while(rest.length){let cut=Math.min(limit,rest.length);if(cut<rest.length){const p=rest.lastIndexOf('\n',cut);const s=rest.lastIndexOf(' ',cut);cut=Math.max(p,s,1)}parts.push(rest.slice(0,cut).trim());rest=rest.slice(cut).trim()}const n=parts.length;return parts.map((p,i)=>`📊 ATS ANALYSIS REPORT (${i+1}/${n})\n${p.replace(/^📊 ATS ANALYSIS REPORT[^\n]*\n/,'')}${i===n-1&&!p.includes('✅ Analysis complete.')?'\n\n✅ Analysis complete.':''}`)} module.exports={chunkMessage};
