const express=require('express');const router=express.Router();const {env}=require('../../config/env');const {handleIncoming}=require('./whatsappHandler');router.get('/whatsapp',(req,res)=>{const token=process.env.WHATSAPP_VERIFY_TOKEN||env.whatsapp.verifyToken;if(req.query['hub.verify_token']===token)return res.status(200).send(req.query['hub.challenge']);return res.sendStatus(403)});router.post('/whatsapp',express.json({limit:'4mb'}),(req,res)=>{res.sendStatus(200);for(const e of req.body.entry||[])for(const c of e.changes||[])for(const m of c.value?.messages||[])handleIncoming({from:m.from,type:m.type,text:m.text,document:m.document}).catch(console.error)});module.exports=router;
