const {clean,tokens}=require('../utils/textCleaner');
const SKILLS=['javascript','typescript','react','node.js','node','express','python','java','sql','mongodb','aws','azure','docker','kubernetes','git','tableau','power bi','excel','kafka','rest','graphql','machine learning','leadership','communication','agile','figma','c++','c#'];
function parseJD(text=''){const raw=String(text);const lower=clean(raw);const found=SKILLS.filter(s=>new RegExp(`\\b${s.replace(/[.+]/g,'\\$&')}\\b`,'i').test(lower));const required=found.filter(s=>new RegExp(`(?:required|must|need|experience with|proficien)\\D{0,40}${s.replace(/[.+]/g,'\\$&')}`,'i').test(lower));const preferred=found.filter(s=>!required.includes(s));const y=raw.match(/(\d+)\+?\s+years?/i);const degree=/phd|doctorate/i.test(raw)?4:/master|mba/i.test(raw)?3:/bachelor|b\.s|b\.tech/i.test(raw)?2:/associate/i.test(raw)?1:0;const title=(raw.match(/(?:job title|position|role)\s*[:\-]\s*([^\n]+)/i)||raw.match(/^(?:senior |junior |lead )?([A-Z][A-Za-z ]+(?:engineer|developer|analyst|manager|scientist))/m)||[])[1]||'Target Role';const keywords=[...new Set([...found,...tokens(raw).filter(x=>x.length>3)])].slice(0,60);const freq=Object.fromEntries(keywords.map(k=>[k,(lower.match(new RegExp(k.replace(/[.+]/g,'\\$&'),'g'))||[]).length]));return {text:raw,role:title.trim(),requiredSkills:required.length?required:found.slice(0,Math.max(1,Math.ceil(found.length/2))),preferredSkills:preferred,yearsRequired:y?Number(y[1]):0,degreeLevel:degree,certifications:(raw.match(/(?:aws|azure|pmp|cissp|scrum|certified)[^,;\n]*/gi)||[]).map(clean),keywords,frequency:freq,softSkills:found.filter(x=>['leadership','communication','agile'].includes(x))}}
module.exports={parseJD};
