const Session=require('../models/Session'); const memory=new Map(); function key(k){return String(k)} async function getSession(k){if(!process.env.MONGODB_URI||process.env.MONGODB_URI.includes('your_'))return memory.get(key(k))||{key:key(k),state:'new'};return Session.findOne({key:key(k)})||{key:key(k),state:'new'}} async function saveSession(k,data){const v={...(await getSession(k)),...data,key:key(k),updatedAt:new Date()};memory.set(key(k),v);if(process.env.MONGODB_URI&&!process.env.MONGODB_URI.includes('your_'))return Session.findOneAndUpdate({key:key(k)},v,{upsert:true,new:true});return v} async function clearSession(k){memory.delete(key(k));if(process.env.MONGODB_URI&&!process.env.MONGODB_URI.includes('your_'))return Session.deleteOne({key:key(k)})} module.exports={getSession,saveSession,clearSession};
