const ABB={js:'javascript',ts:'typescript',ml:'machine learning',ai:'artificial intelligence',nlp:'natural language processing',aws:'amazon web services'}; const STOP=new Set('a an the and or of to in for with on is are be as by from at into using this that'.split(' ')); function clean(t=''){return t.toLowerCase().replace(/\b(js|ts|ml|ai|nlp|aws)\b/g,m=>ABB[m]||m).replace(/[^a-z0-9+#.\-\s]/g,' ').replace(/\s+/g,' ').trim()} function tokens(t){return clean(t).split(/\s+/).filter(x=>x&&!STOP.has(x)&&x.length>1)} module.exports={clean,tokens};
