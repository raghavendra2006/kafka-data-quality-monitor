const {chunkMessage}=require('../../src/platforms/whatsapp/whatsappChunker');describe('WhatsApp chunker',()=>{test('keeps chunks within limit',()=>{const c=chunkMessage(('word '.repeat(1500)).trim());expect(c.length).toBeGreaterThan(1);expect(Math.max(...c.map(x=>x.length))).toBeLessThanOrEqual(1600);expect(c.at(-1)).toContain('✅ Analysis complete.')});test('short output stays single',()=>expect(chunkMessage('hello')).toEqual(['hello']));});
